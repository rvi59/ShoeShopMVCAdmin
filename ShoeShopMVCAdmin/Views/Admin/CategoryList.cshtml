@model IEnumerable<ShoeShopMVCAdmin.Models.tblCategory>

@{
    ViewBag.Title = "CategoryList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Category List</h2>
<br />
<p>
    @Html.ActionLink("Add Category", "CreateCategory", "Admin", null, new { @class = "btn btn-success" })
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CategoryName)
                    </td>
                    <td>
                        @*@Html.ActionLink("Edit", "EditBrand", new { id = item.BrandId })*@
                        <a href="@Url.Action("EditCategory", "Admin", new { id = item.CategoryId })"><i class="fas fa-edit"></i> </a>

                    </td>
                    <td>
                        @Html.ActionLink("Delete", "DeleteCategory", new { id = item.CategoryId }, new { onclick = "return confirm('Are you sure you wish to delete this Brand ?');" })
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td style="text-align:end;">No Record Found</td>
            </tr>
        }
    </tbody>

</table>
